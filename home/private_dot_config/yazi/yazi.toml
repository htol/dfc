[plugin]
fetchers = [
    # Defaults (required for file type detection and preview)
    { id = "mime", group = "mime",   url = "*/",         run = "mime.dir",    prio = "high" },
    { id = "mime", group = "mime",   url = "local://*",  run = "mime.local",  prio = "high" },
    { id = "mime", group = "mime",   url = "remote://*", run = "mime.remote", prio = "high" },
    # Chezmoi managed file highlighting
    { id = "chezmoi", group = "chezmoi", url = "local://*", run = "chezmoi" },
    { id = "chezmoi", group = "chezmoi", url = "*/",        run = "chezmoi" },
]
